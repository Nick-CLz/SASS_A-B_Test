"""Claude-powered agents + tool framework + evals — implemented in P11-P14.

Grounding rule: agents never compute statistics; they call app.stats via tools
and every number is traced (see docs/05-ai-agents.md).
"""
